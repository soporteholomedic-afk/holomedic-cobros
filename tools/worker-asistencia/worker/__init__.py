"""asistencia-rrhh device worker — ZKTeco K20 Pro capture service.

Modules: config (env), zk_client (device border), buffer (SQLite
append-only queue), api_client (HTTPS + Bearer), comandos (command
executor), main (thread entrypoint).
"""
